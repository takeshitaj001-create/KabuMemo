# KabuMemo デプロイ手順書

## 概要

KabuMemo は以下の2コンポーネントで構成されており、それぞれ独立してデプロイする。

| コンポーネント | 技術 | ホスティング先 |
|---|---|---|
| フロントエンド（SPA） | Blazor WebAssembly (.NET 10) | GitHub Pages |
| API プロキシ | Cloudflare Worker | Cloudflare Workers |

### 公開 URL

| 用途 | URL |
|---|---|
| アプリ（トップページ） | https://takeshitaj001-create.github.io/KabuMemo/ |
| 個別銘柄ページ（例：7203） | https://takeshitaj001-create.github.io/KabuMemo/stock/7203 |
| API プロキシ（Worker） | https://kabumemo-proxy.kabumemo.workers.dev |

---

## 前提条件

| 項目 | 確認方法 |
|---|---|
| .NET 10 SDK インストール済み | `dotnet --version` → `10.x.x` |
| Node.js / npm インストール済み | `node --version` |
| Wrangler CLI インストール済み | `worker\` フォルダ内で `npx wrangler --version` |
| Cloudflare アカウントのメール認証済み | Cloudflare ダッシュボードで確認 |
| GitHub リポジトリへのプッシュ権限あり | `git remote -v` で `origin` を確認 |

---

## Part 1 : Cloudflare Worker のデプロイ

### 1-1. Worker フォルダへ移動

```powershell
cd C:\Users\takes\source\repos\KabuMemo\worker
```

### 1-2. Cloudflare にログイン（初回のみ）

```powershell
npx wrangler login
```

ブラウザが開くので Cloudflare アカウントで認証する。

### 1-3. デプロイ実行

```powershell
npx wrangler deploy
```

成功すると以下のように表示される。

```
Uploaded kabumemo-proxy (x.xx sec)
Published kabumemo-proxy (x.xx sec)
  https://kabumemo-proxy.kabumemo.workers.dev
```

### 1-4. 動作確認

ブラウザまたは PowerShell で疎通確認する（`7203` は任意の銘柄コード）。

```powershell
Invoke-RestMethod "https://kabumemo-proxy.kabumemo.workers.dev/api/quote/7203"
```

`quoteResponse.result` に株価データが返れば正常。

---

## Part 2 : フロントエンド（GitHub Pages）のデプロイ

### 2-1. プロジェクトルートへ移動

```powershell
cd C:\Users\takes\source\repos\KabuMemo
```

### 2-2. `index.html` の `<base>` タグについて

GitHub Pages では `<base href="/KabuMemo/" />` が必要だが、**対応方法はデプロイ方式によって異なる。**

| デプロイ方式 | 対応方法 |
|---|---|
| **GitHub Actions（Part 6）** | 手動変更不要。CI が自動で書き換えてビルドする |
| **手動デプロイ（2-5 の方法A〜C）** | ビルド前に手動で変更し、デプロイ後に元に戻す |

#### 手動デプロイの場合のみ実施

```html
<!-- 変更前（ローカル開発用・通常はこの状態を維持） -->
<base href="/" />

<!-- 変更後（GitHub Pages 用・手動デプロイ時のみ変更） -->
<base href="/KabuMemo/" />
```

> **注意:** デプロイ完了後は必ず `/` に戻すこと。戻し忘れるとローカル開発時に画面が表示されなくなる。

### 2-3. リリースビルド実行

```powershell
dotnet publish -c Release
```

出力先: `bin\Release\net10.0\publish\wwwroot\`

### 2-4. SPA ルーティング用に `404.html` を作成

GitHub Pages は存在しないパスへのアクセスを `404.html` にリダイレクトする。  
Blazor SPA のルーティングを機能させるため、`index.html` を `404.html` としてコピーする。

```powershell
Copy-Item "bin\Release\net10.0\publish\wwwroot\index.html"  "bin\Release\net10.0\publish\wwwroot\404.html" -Force
```

### 2-5. ビルド成果物を Git に反映

#### 方法A: `master` ブランチ / docs に成果物を配置する場合（推奨）

GitHub Pages の Source を「master ブランチ / docs」に設定している場合。  
`docs\` フォルダをリポジトリに含めてそのまま master へプッシュするだけでよい。

```powershell
# docs フォルダを作成してビルド成果物をコピー
New-Item -ItemType Directory -Force -Path "docs" | Out-Null
Copy-Item "bin\Release\net10.0\publish\wwwroot\*" "docs\" -Recurse -Force

# 変更をコミット＆プッシュ
git add docs/
git commit -m "deploy: release build"
git push origin master
```

> **メリット:** ブランチを切り替える必要がなく、ソースコードとビルド成果物を同じブランチで管理できる。

#### 方法B: ビルド成果物をリポジトリルートに直接配置する場合

GitHub Pages の Source を「master ブランチ / root」に設定している場合。

```powershell
# ビルド成果物をリポジトリルートにコピー
Copy-Item "bin\Release\net10.0\publish\wwwroot\*" "." -Recurse -Force

# 変更をコミット＆プッシュ
git add -A
git commit -m "deploy: release build"
git push origin master
```

#### 方法C: `gh-pages` ブランチ / root に成果物を配置する場合

GitHub Pages の Source を「gh-pages ブランチ / root」に設定している場合。

```powershell
# gh-pages ブランチが存在しない場合は作成
git checkout --orphan gh-pages
git reset --hard

# ビルド成果物をコピー
Copy-Item "bin\Release\net10.0\publish\wwwroot\*" "." -Recurse -Force

# コミット＆プッシュ
git add -A
git commit -m "deploy: release build"
git push origin gh-pages --force

# master ブランチに戻る
git checkout master
```

#### 方法D: `gh-pages` ブランチ / docs に成果物を配置する場合

GitHub Pages の Source を「gh-pages ブランチ / docs」に設定している場合。

```powershell
# gh-pages ブランチが存在しない場合は作成
git checkout --orphan gh-pages
git reset --hard

# docs フォルダを作成してビルド成果物をコピー
New-Item -ItemType Directory -Force -Path "docs" | Out-Null
Copy-Item "bin\Release\net10.0\publish\wwwroot\*" "docs\" -Recurse -Force

# コミット＆プッシュ
git add -A
git commit -m "deploy: release build"
git push origin gh-pages --force

# master ブランチに戻る
git checkout master
```

---

## Part 3 : GitHub Pages の設定（初回のみ）

1. GitHub リポジトリ（`https://github.com/takeshitaj001-create/KabuMemo`）を開く
2. **Settings** → **Pages** を開く
3. **Source** を以下のように設定する

| 項目 | 設定値 |
|---|---|
| Branch | `master`（または `gh-pages`） |
| Folder | `/ (docs)`（または `root` ）|

4. **Save** をクリック
5. 数分後、`https://takeshitaj001-create.github.io/KabuMemo/` でアクセス可能になる

---

## Part 4 : 動作確認

| 確認項目 | 確認方法 |
|---|---|
| トップページが表示される | `https://takeshitaj001-create.github.io/KabuMemo/` をブラウザで開く |
| 銘柄コードを直接 URL 入力しても表示される | `https://takeshitaj001-create.github.io/KabuMemo/stock/7203` を開く |
| 株価が取得できる | 個別銘柄画面で「🔄 更新」ボタンを押して株価が表示されることを確認 |
| 企業概要・決算予定日が取得できる | 企業概要カードの「🔄 更新」を押してデータが表示されることを確認 |

---

## Part 5 : ローカル開発に戻す際の手順

GitHub Pages デプロイ後にローカル開発を再開する場合、`<base href>` を元に戻す。

```html
<!-- wwwroot\index.html -->
<base href="/" />
```

```powershell
dotnet run
# → http://localhost:5124 で動作確認
```

---

## Part 6 : GitHub Actions による自動デプロイ（推奨）

`master` ブランチへプッシュするたびに自動でビルド・GitHub Pages へデプロイする構成。  
**`<base href>` の手動変更は不要。** CI が自動的に `/KabuMemo/` へ書き換えてビルドする。

### 6-1. ワークフローファイルの配置

`.github/workflows/deploy.yml` を以下の内容で作成する（リポジトリに既に含まれている場合は不要）。

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Set base href for GitHub Pages
        run: |
          sed -i 's|<base href="/" />|<base href="/KabuMemo/" />|g' wwwroot/index.html

      - name: Publish
        run: dotnet publish -c Release

      - name: Copy index.html to 404.html
        run: |
          cp bin/Release/net10.0/publish/wwwroot/index.html \
             bin/Release/net10.0/publish/wwwroot/404.html

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: bin/Release/net10.0/publish/wwwroot
          destination_dir: docs   # gh-pages ブランチの /docs フォルダへ配置
          force_orphan: true
```

### 6-2. GitHub Pages の Source を gh-pages ブランチに設定（初回のみ）

`destination_dir: docs` を指定しているため、GitHub Pages の配信元は `gh-pages` ブランチの `/docs` フォルダになる。

1. GitHub リポジトリの **Settings** → **Pages** を開く
2. **Source** を以下のように設定して **Save**

| 項目 | 設定値 |
|---|---|
| Branch | `gh-pages` |
| Folder | `/docs` |

> **`/ (root)` と `/docs` の違い**
>
> | 設定 | deploy.yml の `destination_dir` | gh-pages ブランチの構造 |
> |---|---|---|
> | `/ (root)` | 不要（省略） | ブランチ直下にファイルが配置される |
> | `/docs` | `destination_dir: docs` | `docs/` フォルダ内にファイルが配置される |
>
> GitHub Pages の Settings と deploy.yml の設定は必ず一致させること。

### 6-3. 通常のデプロイ手順（自動化後）

```powershell
git add -A
git commit -m "コミットメッセージ"
git push origin master
```

プッシュ後、GitHub の **Actions** タブで `Deploy to GitHub Pages` ワークフローが緑（✅）になれば完了。

> **注意:** Actions 失敗時は **Settings** → **Actions** → **General** → **Workflow permissions** が  
> 「Read and write permissions」になっているか確認する。

---

## トラブルシューティング

### Worker デプロイ時に `code: 10034` エラー

Cloudflare アカウントのメールアドレスが未認証。  
`https://dash.cloudflare.com/` にログインしてメール認証を完了させる。

### GitHub Pages にアクセスすると 404 になる

- `wwwroot\index.html` の `<base href="/KabuMemo/" />` が設定されているか確認
- `404.html` が `wwwroot` 直下にコピーされているか確認
- GitHub Pages の Settings で正しいブランチが選択されているか確認

### 株価・企業概要が取得できない

- Worker が正常にデプロイされているか `Part 1-4` の疎通確認を実施
- `Services\StockApiService.cs` の `ProxyBaseUrl` が本番 Worker の URL になっているか確認

```csharp
private const string ProxyBaseUrl = "https://kabumemo-proxy.kabumemo.workers.dev";
```
