# KabuMemo 詳細設計書

## 1. システム概要

### 1-1. 目的

株式投資に関するメモ・銘柄情報を一元管理する個人向け Web アプリ。  
サーバーサイド処理を持たない静的 SPA として GitHub Pages で動作し、データは全てブラウザの localStorage に保存する。

### 1-2. システム構成

```
[ブラウザ]
  Blazor WebAssembly (.NET 10)
  ├─ 銘柄一覧画面 (Home.razor)
  ├─ 個別銘柄詳細画面 (StockDetail.razor)
  ├─ IR情報一覧画面 (Disclosures.razor)
  └─ 設定画面 (Settings.razor)
         ↓ HTTP (fetch)
[Cloudflare Worker]
  kabumemo-proxy.kabumemo.workers.dev
  ├─ Cookie + Crumb 認証管理
  ├─ CORS ヘッダー付与
  └─ レート制御・フォールバック
         ↓
[外部 API]
  ├─ Yahoo Finance v7/v8/v10 (株価・チャート・配当・企業概要)
  ├─ Yahoo Finance Search v1  (銘柄コード検索)
  ├─ Yahoo Finance Japan      (銘柄名検索)
  ├─ irbank.net               (適時開示スクレイピング)
  └─ MyMemory API             (英日翻訳)

[GitHub API]  ← GitHubService が直接呼び出す（Worker 経由なし）
  github.com/api/repos/takeshitaj001-create/KabuMemo-shared
  └─ shared-md/ フォルダ（MD ファイル共有）
```

### 1-3. 技術スタック

| 区分 | 技術 | バージョン |
|---|---|---|
| フロントエンド | Blazor WebAssembly | .NET 10.0.7 |
| スタイリング | Bootstrap 5 + カスタム CSS | - |
| Markdown レンダリング | Markdig | - |
| API プロキシ | Cloudflare Workers | - |
| ホスティング | GitHub Pages | - |
| データ永続化 | localStorage (JS Interop) | - |
| CI/CD | GitHub Actions | - |

---

## 2. ディレクトリ構成

```
KabuMemo/
├── App.razor                        # ルートコンポーネント（Router 定義）
├── Program.cs                       # DI 設定・エントリーポイント
├── _Imports.razor                   # グローバル using
├── KabuMemo.csproj                  # プロジェクト定義
│
├── Pages/
│   ├── Home.razor                   # 銘柄一覧画面
│   ├── StockDetail.razor            # 個別銘柄詳細画面
│   ├── Disclosures.razor            # IR情報一覧画面
│   ├── Settings.razor               # GitHub PAT 設定画面
│   └── NotFound.razor               # 404 ページ
│
├── Models/
│   ├── StockItem.cs                 # 銘柄データモデル
│   └── DisclosureItem.cs            # 適時開示情報モデル・キャッシュモデル
│
├── Services/
│   ├── StockService.cs              # localStorage 操作
│   ├── StockApiService.cs           # 外部 API 呼び出し（Cloudflare Worker経由）
│   ├── DisclosureService.cs         # IR情報取得・キャッシュ管理
│   └── GitHubService.cs             # GitHub API 経由のMD共有
│
├── Layout/
│   ├── MainLayout.razor             # 共通レイアウト
│   ├── MainLayout.razor.css
│   ├── NavMenu.razor                # ナビゲーション
│   └── NavMenu.razor.css
│
├── wwwroot/
│   ├── index.html                   # SPA エントリー HTML
│   ├── 404.html                     # GitHub Pages SPA ルーティング対応
│   ├── css/app.css                  # カスタムスタイル
│   └── lib/bootstrap/              # Bootstrap 5
│
└── worker/
    ├── index.js                     # Cloudflare Worker 本体
    ├── wrangler.toml                # Worker 設定
    └── package.json
```

---

## 3. データモデル

### 3-1. StockItem（銘柄情報）

`Models/StockItem.cs`

| プロパティ | 型 | 説明 |
|---|---|---|
| `Code` | `string` | 銘柄コード（例: "7203"） |
| `Name` | `string` | 銘柄名（例: "トヨタ自動車"） |
| `Memo` | `string` | ユーザーメモ |
| `AlarmDate` | `string?` | アラーム日付（YYYY-MM-DD） |
| `IsBuyCandidate` | `bool` | 購入検討フラグ |
| `IsHolding` | `bool` | 保有中フラグ |
| `CurrentPrice` | `decimal?` | 現在値（円） |
| `PreviousClose` | `decimal?` | 前日終値（円） |
| `PriceChange` | `decimal?` | 前日比（円） |
| `PriceChangeRate` | `decimal?` | 前日比率（%） |
| `PriceDirection` | `string` | "up" / "down" / "flat" |
| `OpenPrice` | `decimal?` | 始値 |
| `HighPrice` | `decimal?` | 高値 |
| `LowPrice` | `decimal?` | 安値 |
| `Volume` | `long?` | 出来高 |
| `LastUpdated` | `string?` | 最終株価取得日時（"yyyy-MM-dd HH:mm"） |
| `Exchange` | `string?` | 市場名 |
| `MarketCap` | `long?` | 時価総額 |
| `LotSize` | `int?` | 単元株数 |
| `ForwardPE` | `decimal?` | PER（会社予想） |
| `PriceToBook` | `decimal?` | PBR |
| `EPS` | `decimal?` | EPS（1株利益） |
| `BPS` | `decimal?` | BPS（1株純資産） |
| `DividendYield` | `decimal?` | 配当利回り（%） |
| `DividendRate` | `decimal?` | 1株配当（円） |
| `WeekHigh52` | `decimal?` | 52週高値 |
| `WeekLow52` | `decimal?` | 52週安値 |
| `CompanyDescription` | `string?` | 企業概要（日本語翻訳済み） |
| `Employees` | `int?` | 従業員数 |
| `CompanyCity` | `string?` | 本社所在都市 |
| `CompanyCountry` | `string?` | 本社所在国 |
| `NextEarningsDate` | `string?` | 次回決算予定日（YYYY-MM-DD） |
| `DividendHistory` | `List<DividendEntry>?` | 配当推移（過去5年） |
| `ChartData` | `List<ChartPoint>?` | 株価チャートデータ（1年日次） |
| `MdContent` | `string?` | MDメモ（Markdownテキスト） |

#### ネスト型: DividendEntry

| プロパティ | 型 | 説明 |
|---|---|---|
| `Year` | `int` | 年度（4月始まり） |
| `Amount` | `decimal` | 年間配当合計（円） |
| `InterimAmount` | `decimal` | 中間配当（7月以降払い） |
| `FinalAmount` | `decimal` | 期末配当（1〜6月払い） |

#### ネスト型: ChartPoint

| プロパティ | 型 | 説明 |
|---|---|---|
| `Date` | `string` | 日付（YYYY-MM-DD） |
| `Close` | `decimal` | 終値（円） |

### 3-2. DisclosureItem（適時開示情報）

`Models/DisclosureItem.cs`

| プロパティ | 型 | 説明 |
|---|---|---|
| `Code` | `string` | 銘柄コード |
| `StockName` | `string` | 銘柄名 |
| `Date` | `string` | 発表日（YYYY-MM-DD） |
| `Time` | `string` | 発表時刻（HH:mm、空の場合あり） |
| `Title` | `string` | タイトル |
| `Category` | `string` | カテゴリ（タイトルから自動推定） |
| `Url` | `string` | irbank.net へのリンク URL |

カテゴリ推定ルール（`InferCategory`）：
- タイトルに「決算」→ "決算"
- タイトルに「配当」→ "配当"
- タイトルに「業績」「修正」→ "業績"
- タイトルに「株主総会」→ "株主総会"
- タイトルに「自己株」→ "自己株式"
- タイトルに「合併」「買収」「TOB」→ "M&A"
- タイトルに「役員」「代表取締役」→ "役員"
- タイトルに「増資」「株式分割」→ "資本政策"
- その他 → "開示"

### 3-3. DisclosureCache（IR情報キャッシュ）

`Models/DisclosureItem.cs` 内で定義

| プロパティ | 型 | 説明 |
|---|---|---|
| `FetchedAt` | `DateTime` | 最終取得日時 |
| `Items` | `List<DisclosureItem>` | キャッシュされた開示一覧（全銘柄） |

### 3-4. GitHubFileInfo（共有MDファイル情報）

`Services/GitHubService.cs` 内で定義

| プロパティ | 型 | 説明 |
|---|---|---|
| `Name` | `string` | ファイル名（例: "memo.md"） |
| `Sha` | `string` | GitHub コンテンツ SHA（更新時に使用） |
| `Path` | `string` | リポジトリ内パス（例: "shared-md/memo.md"） |

### 3-5. localStorage スキーマ

```
キー: "kabumemo_stocks"
値: JSON配列（List<StockItem> のシリアライズ）

キー: "kabumemo_github_pat"
値: GitHub Personal Access Token（平文）

キー: "kabumemo_disclosures"
値: JSON（DisclosureCache のシリアライズ）

キー: "kabumemo_disclosures_seen"
値: 最終既読日時文字列（"yyyy-MM-dd HH:mm"）
```

---

## 4. サービス層

### 4-1. StockService（localStorage 操作）

`Services/StockService.cs`

#### 概要

localStorage への銘柄データの読み書きを担当。JS Interop 経由でブラウザの localStorage を操作する。

#### DI 登録

```csharp
builder.Services.AddScoped<StockService>();
```

#### メソッド

| メソッド | 戻り値 | 説明 |
|---|---|---|
| `GetAllAsync()` | `Task<List<StockItem>>` | localStorage から全銘柄を読み込む |
| `SaveAsync(List<StockItem>)` | `Task` | 全銘柄を localStorage に書き込む |

#### 実装詳細

```
GetAllAsync:
  1. JS.InvokeAsync<string>("localStorage.getItem", "kabumemo_stocks")
  2. null または例外 → 空リストを返す
  3. JSON.Deserialize<List<StockItem>> で復元

SaveAsync:
  1. JSON.Serialize(items)
  2. JS.InvokeVoidAsync("localStorage.setItem", "kabumemo_stocks", json)
```

### 4-2. StockApiService（外部 API 呼び出し）

`Services/StockApiService.cs`

#### 概要

Cloudflare Worker を経由して Yahoo Finance・irbank.net からデータを取得する。  
HttpClient のタイムアウトは 10 秒。

#### DI 登録

```csharp
builder.Services.AddScoped(_ => new StockApiService(
    new HttpClient { Timeout = TimeSpan.FromSeconds(10) }
));
```

#### エンドポイント一覧

| メソッド | Worker エンドポイント | 戻り値 | 説明 |
|---|---|---|---|
| `FetchQuoteAsync(code)` | `GET /api/quote/{code}` | `QuoteResult` | 現在値・各種指標 |
| `FetchChartAsync(code)` | `GET /api/chart/{code}` | `ChartResult` | 日次株価（1年分） |
| `FetchDividendsAsync(code)` | `GET /api/dividends/{code}` | `DividendResult` | 配当推移（5年） |
| `FetchSummaryAsync(code)` | `GET /api/summary/{code}` | `SummaryResult` | 企業概要・決算日 |
| `FetchSearchResultsAsync(query)` | `GET /api/name/{query}` | `List<YahooSearchQuote>` | 銘柄検索 |
| `FetchJapaneseNameAsync(code)` | `GET /api/name/{code}` | `string?` | 銘柄和名取得 |

#### 結果ラッパー型

全 API の戻り値は以下のパターンに統一されている。

```csharp
class XxxResult {
  bool Success      // データ取得成功かどうか
  TData? Data       // 成功時のデータ
  string? Error     // 失敗時のエラーメッセージ
}
```

| 型 | 成功時のデータ型 |
|---|---|
| `QuoteResult` | `StockQuote` |
| `ChartResult` | `List<ChartPoint>` |
| `DividendResult` | `List<DividendEntry>` |
| `SummaryResult` | `CompanySummaryResponse` |

#### エラーハンドリング

各メソッドは以下の例外をキャッチし、`Result.Fail()` に変換する。

| 例外 | エラーメッセージ |
|---|---|
| `HttpRequestException` | `HTTP エラー(HTTP {status}): {message}` |
| `TaskCanceledException` | `タイムアウト（10秒）` |
| `Exception` | `{ExceptionType}: {message}` |

### 4-3. DisclosureService（IR情報取得・キャッシュ）

`Services/DisclosureService.cs`

#### 概要

Cloudflare Worker の `/api/disclosures/{code}` エンドポイントから適時開示情報を取得し、  
localStorage にキャッシュする。HttpClient のタイムアウトは 15 秒。

#### DI 登録

```csharp
builder.Services.AddScoped<DisclosureService>(sp => new DisclosureService(
    new HttpClient { Timeout = TimeSpan.FromSeconds(15) },
    sp.GetRequiredService<IJSRuntime>()
));
```

#### メソッド

| メソッド | 戻り値 | 説明 |
|---|---|---|
| `GetCacheAsync()` | `Task<DisclosureCache?>` | localStorage からキャッシュを読み込む |
| `IsCacheValid(cache)` | `bool` | キャッシュが1時間以内かを確認 |
| `SaveCacheAsync(items)` | `Task` | localStorage にキャッシュを書き込む |
| `GetLastSeenAsync()` | `Task<string>` | 最終既読日時を取得 |
| `MarkAllSeenAsync()` | `Task` | 全件を既読マークする |
| `GetRecentAsync(stock, count)` | `Task<List<DisclosureItem>>` | 特定銘柄の最新N件を取得（キャッシュ優先） |
| `FetchAllAsync(stocks, onProgress)` | `Task<(items, errors)>` | 全銘柄の開示情報を逐次取得（400ms間隔） |

#### キャッシュ戦略

```
GetRecentAsync:
  1. キャッシュが有効（1時間以内）かつ対象銘柄のデータが存在
     → キャッシュから返す
  2. それ以外
     → FetchForStockAsync() で個別取得
```

### 4-4. GitHubService（MD共有）

`Services/GitHubService.cs`

#### 概要

GitHub API を直接呼び出し（Cloudflare Worker 経由なし）、  
固定リポジトリ（`takeshitaj001-create/KabuMemo-shared`）の `shared-md/` フォルダで  
MD ファイルを共有する。PAT（Personal Access Token）は localStorage に保存。

#### 定数

| 定数 | 値 | 説明 |
|---|---|---|
| `Owner` | `"takeshitaj001-create"` | GitHub オーナー名（固定・変更不可） |
| `Repo` | `"KabuMemo-shared"` | リポジトリ名（固定・変更不可） |
| `Folder` | `"shared-md"` | 共有フォルダ名（固定・変更不可） |

#### DI 登録

```csharp
builder.Services.AddScoped<GitHubService>();
```

#### メソッド

| メソッド | 戻り値 | 説明 |
|---|---|---|
| `GetPatAsync()` | `Task<string?>` | localStorage から PAT を取得 |
| `SavePatAsync(pat)` | `Task` | localStorage に PAT を保存 |
| `DeletePatAsync()` | `Task` | localStorage から PAT を削除 |
| `ListFilesAsync()` | `Task<(Files, Error)>` | 共有フォルダの MD ファイル一覧取得 |
| `ListFilesWithPatAsync(pat)` | `Task<(Files, Error)>` | 指定 PAT でファイル一覧取得（接続テスト用） |
| `UploadFileAsync(filename, content)` | `Task<string?>` | MD ファイルをアップロード（既存時は SHA で更新） |
| `DownloadFileAsync(path)` | `Task<(Content, Error)>` | MD ファイルをダウンロード（Base64 デコード） |

#### 必要な PAT 権限

Contents（Read and write）

---

## 5. 画面設計

### 5-1. 銘柄一覧画面（Home.razor）

**ルート:** `@page "/"`

#### レイアウト

```
┌─────────────────────────────────────────────────┐
│ [ツールバー] 銘柄一覧 N件 | [更新時刻] [📊株価更新] [＋追加] │
├─────────────────────────────────────────────────┤
│ [凡例] アラーム日付が本日以前の銘柄（ハイライト表示）   │
├─────────────────────────────────────────────────┤
│ [銘柄テーブル]                                     │
│ 詳細 | ↑↓ | 銘柄 | 現在値 | 外部リンク | メモ | アラーム日 | 削除 │
│ ...                                              │
├─────────────────────────────────────────────────┤
│ [エラーログパネル] ← 失敗時のみ表示                  │
└─────────────────────────────────────────────────┘
```

#### 状態変数

| 変数 | 型 | 初期値 | 説明 |
|---|---|---|---|
| `stocks` | `List<StockItem>` | `[]` | 表示中の銘柄リスト |
| `showAddModal` | `bool` | `false` | 追加モーダル表示フラグ |
| `showDeleteConfirm` | `bool` | `false` | 削除確認モーダル表示フラグ |
| `deleteTargetCode` | `string` | `""` | 削除対象銘柄コード |
| `addTab` | `int` | `0` | 追加タブ（0=名称検索, 1=コード入力） |
| `newCode` | `string` | `""` | 入力中の銘柄コード |
| `newName` | `string` | `""` | 入力中の銘柄名 |
| `addError` | `string` | `""` | 追加時のエラーメッセージ |
| `nameSearchError` | `string` | `""` | 名称検索エラーメッセージ |
| `nameSearchQuery` | `string` | `""` | 名称検索クエリ |
| `nameSearchResults` | `List<YahooSearchQuote>` | `[]` | 名称検索結果 |
| `nameSearchDone` | `bool` | `false` | 名称検索完了フラグ |
| `isUpdatingPrices` | `bool` | `false` | 株価更新中フラグ |
| `isSearchingName` | `bool` | `false` | 名称検索中フラグ |
| `isSearchingByName` | `bool` | `false` | 名前→コード検索中フラグ |
| `priceApiError` | `bool` | `false` | 株価API失敗フラグ |
| `updatedCount` | `int` | `0` | 更新済み銘柄数 |
| `lastPriceUpdateTime` | `string` | `""` | 最終株価更新時刻（"HH:mm"） |
| `errorLog` | `List<string>` | `[]` | API エラーログ |

#### 主要メソッド

| メソッド | 処理概要 |
|---|---|
| `OnInitializedAsync` | `StockService.GetAllAsync()` で銘柄リスト読み込み。`lastPriceUpdateTime` を最新の `LastUpdated` から取得 |
| `OnAfterRenderAsync` | 初期化完了後1回だけ `AutoUpdateIfStaleAsync()` を起動 |
| `AutoUpdateIfStaleAsync` | 最終更新から30分以上経過している場合に `UpdateAllPrices()` を自動実行 |
| `UpdateAllPrices` | 全銘柄を200ms間隔で逐次 `FetchQuoteAsync()` → `SaveAsync()` |
| `AddStock` | `newCode` / `newName` 入力値を `stocks` に追加 → 保存 |
| `SearchByName` | `FetchSearchResultsAsync(query)` → `nameSearchResults` に格納（Enter キー対応） |
| `SelectSearchResult` | 検索結果の銘柄を選択し `newCode` / `newName` に反映、コード入力タブに切り替え |
| `SearchStockName` | `FetchJapaneseNameAsync(code)` → `newName` に反映 |
| `DeleteStock` | `deleteTargetCode` をセットして確認モーダル表示 |
| `ConfirmDelete` | `stocks` から対象を除去 → 保存 |
| `MoveUp` / `MoveDown` | リスト内の順序を入れ替え → 保存 |
| `IsAlarm` (static) | `AlarmDate` が本日以前かどうかを判定 |
| `NavToDetail` | `Nav.NavigateTo("stock/" + code)` で詳細画面へ遷移 |

#### テーブルのハイライトルール

```csharp
static bool IsAlarm(StockItem item)
  => !string.IsNullOrEmpty(item.AlarmDate)
     && string.Compare(item.AlarmDate, DateTime.Today.ToString("yyyy-MM-dd"),
                       StringComparison.Ordinal) <= 0;
```

#### 株価一括更新フロー

```
UpdateAllPrices():
  isUpdatingPrices = true
  priceApiError = false
  foreach (stock in stocks):
    result = await FetchQuoteAsync(stock.Code)
    if result.Success:
      stock.CurrentPrice / PriceChange / LastUpdated 等を更新
    else:
      failCount++
      errorLog.Add("[HH:mm:ss] {result.Error}")
    updatedCount++
    await Task.Delay(200)   ← API 負荷軽減
  await SaveAsync()
  lastPriceUpdateTime = DateTime.Now.ToString("HH:mm")
  priceApiError = failCount > 0
  isUpdatingPrices = false
```

#### テーブルの外部リンク列

各銘柄行に3つのリンクアイコンを表示：

| ボタン | リンク先 |
|---|---|
| Y! | `https://finance.yahoo.co.jp/quote/{Code}` |
| みん | `https://minkabu.jp/stock/{Code}` |
| 株探 | `https://kabutan.jp/stock/?code={Code}` |

### 5-2. 個別銘柄詳細画面（StockDetail.razor）

**ルート:** `@page "/stock/{Code}"`

#### レイアウト

```
┌─ 戻るバー ──────────────────────────────────────────┐
│ [← 一覧に戻る]  一覧 › CODE 銘柄名                    │
├─ ヘッダー ─────────────────────────────────────────┤
│ CODE / 銘柄名 / 現在値 ▲前日比 ▲前日比率               │
├─ 外部リンク行（3つ）───────────────────────────────┤
│ [ヤフーファイナンス] [みんかぶ] [株探（かぶたん）]        │
├─ メモカード ───────────────────────────────────────┤
│ テキストエリア（6行）                                  │
│ タグ:[購入検討][保有中][⚠アラーム]  アラーム日 入力      │
├─ IRカード ─────────────────────────────────────────┤
│ IR情報（最新3件）  [🔄更新] [IR情報一覧 →]             │
│ ─────────────────────────────────────────────── │
│ 日付 カテゴリ タイトル (リンク)                          │
├─ 3カラムレイアウト ──────────────────────────────────┤
│ [基本情報カード]     [株価情報カード]    [企業概要カード] │
│ 市場情報             [SVGチャート]       企業概要（日本語）│
│ 決算                 1M 3M 6M 1Y        従業員数       │
│ 株価指標             始値/高値/安値      所在地          │
│ 配当                 前終値/出来高       [🔄更新]        │
│ 52週レンジ           取得日時                           │
├─ 配当チャートカード ────────────────────────────────┤
│ CAGR表示  [🔄更新]                                    │
│ 積み上げ棒グラフ（5年間）                               │
├─ MDメモカード ─────────────────────────────────────┤
│ [📄ローカル取込] [☁共有から選択] [✕クリア]              │
│ Markdownレンダリング表示                               │
└────────────────────────────────────────────────────┘
```

#### 状態変数

| 変数 | 型 | 説明 |
|---|---|---|
| `Code` | `string` | ルートパラメータ（銘柄コード） |
| `stock` | `StockItem?` | 表示中の銘柄データ |
| `allStocks` | `List<StockItem>` | 全銘柄リスト（保存時に使用） |
| `recentDisclosures` | `List<DisclosureItem>` | 直近3件のIR情報 |
| `isLoadingDisclosures` | `bool` | IR情報取得中フラグ |
| `isRefreshing` | `bool` | 株価取得中フラグ |
| `priceError` | `bool` | 株価取得エラーフラグ |
| `priceErrorMessage` | `string` | 株価取得エラーメッセージ |
| `isRefreshingChart` | `bool` | チャート取得中フラグ |
| `isRefreshingSummary` | `bool` | 企業概要取得中フラグ |
| `summaryError` | `bool` | 企業概要取得エラーフラグ |
| `summaryErrorMessage` | `string` | 企業概要取得エラーメッセージ |
| `isRefreshingDividends` | `bool` | 配当取得中フラグ |
| `dividendError` | `bool` | 配当取得エラーフラグ |
| `dividendErrorMessage` | `string` | 配当取得エラーメッセージ |
| `chartPeriod` | `string` | チャート期間 ("1M"/"3M"/"6M"/"1Y")、初期値 "3M" |
| `tooltipIndex` | `int?` | チャートツールチップ表示インデックス |
| `chartSvgRef` | `ElementReference` | SVG 要素の参照（座標計算に使用） |
| `showSharedMdModal` | `bool` | 共有MDモーダル表示フラグ |
| `sharedFiles` | `List<GitHubFileInfo>` | 共有ファイル一覧 |
| `sharedFilesError` | `string` | ファイル一覧取得エラー |
| `isLoadingFiles` | `bool` | ファイル一覧取得中フラグ |
| `isImporting` | `bool` | ファイル取込中フラグ |
| `importingFile` | `string` | 取込中のファイル名 |
| `uploadFileName` | `string` | アップロード選択中のファイル名 |
| `uploadContent` | `string` | アップロード内容（テキスト） |
| `uploadSaveName` | `string` | アップロード保存名 |
| `isUploading` | `bool` | アップロード中フラグ |
| `uploadMessage` | `string` | アップロード結果メッセージ |
| `uploadError` | `bool` | アップロードエラーフラグ |
| `sharedFilesFilter` | `string` | ファイル一覧の絞り込み文字列 |

#### 初期化フロー

```
OnInitializedAsync():
  1. StockService.GetAllAsync() で全銘柄取得
  2. stock = allStocks.FirstOrDefault(s => s.Code == Code)
  3. 並行して以下を fire-and-forget で起動:
     - RefreshPrice()             ← 毎回取得
     - RefreshChart()             ← 毎回取得
     - RefreshSummary()           ← stock.CompanyDescription が空の場合のみ
     - RefreshDividends()         ← stock.DividendHistory が null の場合のみ
     - LoadRecentDisclosuresAsync() ← 毎回取得（キャッシュ優先）
```

#### 株価チャート（SVG インタラクティブ）

`GetFilteredChartData()` → `BuildChartPoints()` で SVG polyline を生成する。

```
フィルタ:
  "1M" → 直近30日
  "3M" → 直近90日（デフォルト）
  "6M" → 直近180日
  "1Y" → 全データ（1年分）

SVGビューポート: 320×130 (viewBox)、表示サイズ: width:100% height:240px
プロット領域: X:[46, 314]  Y:[8, 100]

折れ線 + エリア塗りつぶし:
  - 上昇（終値 >= 始値）: stroke #0066cc、area rgba(0,102,204,0.12)
  - 下落（終値 < 始値）:  stroke #e03535、area rgba(224,53,53,0.1)

Y軸グリッドライン: 25%刻みで5本（min, 25%, 50%, 75%, max）
X軸ラベル: 期間に応じて週/月/四半期の区切りで自動表示

ツールチップ（マウス/タッチ対応）:
  - JS.InvokeAsync<double[]>("getSvgCoords", chartSvgRef, clientX, clientY)
    でクライアント座標をSVG座標に変換
  - 最近傍インデックスを tooltipIndex に格納
  - 縦線 + 円 + バブル（日付・終値）を SVG でオーバーレイ表示
```

#### 配当推移チャート（SVG 積み上げ棒グラフ）

```
データ: DividendHistory（最大5年）
SVGビューポート: 420×185

棒グラフ:
  - 中間配当（7月以降払い）: #64b5f6（薄青）
  - 期末配当（1〜6月払い）:  #1e88e5（濃青）
  - InterimAmount と FinalAmount が両方 > 0 の場合のみ分割表示
    それ以外は単色（#1e88e5）で合計額を表示

CAGR 計算:
  - 当年度（currentFiscalYear = 今年4月始まり）を除いた過去年度で計算
  - CAGR = (最終年 / 初年) ^ (1 / (年数-1)) - 1
  - filtered.Count < 2 の場合は "—" を表示

ラベル:
  - 過去年度: "YY年度"（例: "23年度"）
  - 現在進行中の年度: "YY年度" + "(現時点)"（#e67e22で表示）
```

#### MDメモカード

```
取込方法:
  1. ローカル取込: <InputFile> でファイル選択 → StreamReader で読み込み
     - .md / .txt を許容
     - 上書き時は JS confirm で確認
  2. 共有から選択: GitHubService.ListFilesAsync() → モーダルでファイル選択
     → GitHubService.DownloadFileAsync() でダウンロード

レンダリング:
  - Markdig.Markdown.ToHtml() でHTML変換
  - UseAdvancedExtensions() + DisableHtml()（インラインHTMLは無効化）

共有MDモーダル:
  - アップロードセクション: .md ファイルを選択してGitHubにアップロード
    - 上限サイズ: 100KB
    - 既存ファイルの場合は SHA を使って更新
  - ファイル一覧セクション: 一覧から選択して取込
    - ファイル名による絞り込みフィルタあり
```

#### IR情報カード

```
表示件数: 最新3件
取得元: DisclosureService.GetRecentAsync(stock, 3)
  → キャッシュ（1時間以内）があればキャッシュを使用
  → なければ Cloudflare Worker /api/disclosures/{code} を呼び出し

表示項目: 日付 / カテゴリ / タイトル（irbank.netへのリンク）
[IR情報一覧 →] ボタン: /disclosures?code={Code} へ遷移
```

#### 外部リンク

| リンク名 | URL パターン |
|---|---|
| ヤフーファイナンス | `https://finance.yahoo.co.jp/quote/{Code}` |
| みんかぶ | `https://minkabu.jp/stock/{Code}` |
| 株探（かぶたん） | `https://kabutan.jp/stock/?code={Code}` |

### 5-3. IR情報一覧画面（Disclosures.razor）

**ルート:** `@page "/disclosures"`

**クエリパラメータ:** `?code={銘柄コード}` （遷移元が指定した場合に自動フィルタ）

#### レイアウト

```
┌─ トップバー ────────────────────────────────────────┐
│ [← 一覧に戻る]  IR情報一覧                            │
│ 最終取得: HH:mm  次回更新: HH:mm  [自動更新：ON/OFF] [🔄更新] │
├─ エラーパネル（失敗時のみ）──────────────────────────┤
├─ フィルター ──────────────────────────────────────┤
│ [銘柄選択ドロップダウン]                               │
├─ プログレスバー（取得中のみ）────────────────────────┤
├─ 開示リスト ──────────────────────────────────────┤
│ [日付 時刻] [銘柄コード] [銘柄名] [カテゴリ] [NEW]      │
│ タイトル（irbank.netへのリンク）                        │
│ ...                                                │
└────────────────────────────────────────────────────┘
```

#### 状態変数

| 変数 | 型 | 説明 |
|---|---|---|
| `InitialCode` | `string?` | クエリパラメータ `?code=` の値 |
| `stocks` | `List<StockItem>` | 登録銘柄リスト |
| `cache` | `DisclosureCache?` | 最後に取得したキャッシュ |
| `allItems` | `List<DisclosureItem>` | 全銘柄の開示一覧（日付降順） |
| `errors` | `List<string>` | 取得エラーリスト |
| `filterCode` | `string` | フィルター選択中の銘柄コード（空=全銘柄） |
| `lastSeen` | `string` | 最終既読日時（"yyyy-MM-dd HH:mm"） |
| `nextRefreshAt` | `string` | 次回自動更新時刻（"HH:mm"） |
| `isFetching` | `bool` | 取得中フラグ |
| `fetchDone` | `int` | 取得完了銘柄数 |
| `fetchTotal` | `int` | 取得対象銘柄数 |
| `autoRefreshEnabled` | `bool` | 自動更新ON/OFFフラグ（初期値: true） |
| `cts` | `CancellationTokenSource?` | 自動更新タスクのキャンセルトークン |

#### 初期化フロー

```
OnInitializedAsync():
  1. StockService.GetAllAsync() で登録銘柄取得
  2. DisclosureService.GetCacheAsync() でキャッシュ読み込み
  3. GetLastSeenAsync() で最終既読日時取得
  4. InitialCode があれば filterCode にセット

OnAfterRenderAsync(firstRender=true 相当):
  1. InitialRefreshIfStaleAsync() を起動（1回のみ）

InitialRefreshIfStaleAsync():
  1. キャッシュが5分以上古い → RefreshAsync() を実行
  2. RunScheduledRefreshAsync() を起動（バックグラウンド）
```

#### 自動更新ロジック

```
RunScheduledRefreshAsync():
  while (!キャンセル):
    1. 次の「5分境界」（:00/:05/:10...）まで待機
    2. nextRefreshAt を更新して表示
    3. Task.Delay(delay, cts.Token) で待機
    4. RefreshAsync() を実行
    5. nextRefreshAt を再計算して表示

CalcDelayToNext5Min():
  次の5分刻みの時刻（例: 現在12:13 → 次は12:15）までの TimeSpan を返す
```

#### 開示アイテムのハイライト

| 条件 | クラス | 表示 |
|---|---|---|
| `item.Date == 今日の日付` | `disc-item-today` | 背景色変更 |
| `item.Date + Time > lastSeen` | `disc-item-new` | NEW バッジ表示 |

#### カテゴリ CSS クラスマッピング（`CatClass`）

| カテゴリ | CSS クラス |
|---|---|
| 決算 | `disc-cat-kessan` |
| 配当 | `disc-cat-haito` |
| 業績 | `disc-cat-gyoseki` |
| M&A | `disc-cat-ma` |
| 株主総会 | `disc-cat-soukai` |
| その他 | `disc-cat-other` |

### 5-4. 設定画面（Settings.razor）

**ルート:** `@page "/settings"`

#### レイアウト

```
┌─ 戻るバー ─────────────────────────────────────────┐
│ [← 一覧に戻る]  設定                                 │
├─ GitHub共有設定カード ────────────────────────────┤
│ Personal Access Token (PAT) [パスワード入力] [✓保存済み/⚠未設定] │
│ オーナー: takeshitaj001-create（変更不可）               │
│ リポジトリ名: KabuMemo-shared（変更不可）               │
│ 共有フォルダ名: shared-md（変更不可）                   │
│ [保存] [接続テスト] [🗑 PAT削除]                         │
│ [結果メッセージ]                                        │
└────────────────────────────────────────────────────┘
```

#### 状態変数

| 変数 | 型 | 説明 |
|---|---|---|
| `pat` | `string` | 入力中の PAT |
| `isBusy` | `bool` | 処理中フラグ |
| `busyLabel` | `string` | 処理中の種別（"save"/"test"/"delete"） |
| `message` | `string` | 結果メッセージ |
| `isError` | `bool` | エラーメッセージフラグ |
| `isPatStored` | `bool` | PAT 保存済みフラグ |

#### 主要メソッド

| メソッド | 処理概要 |
|---|---|
| `OnInitializedAsync` | `GhSvc.GetPatAsync()` で保存済み PAT を読み込み |
| `SaveAsync` | PAT を localStorage に保存 |
| `DeleteAsync` | PAT を localStorage から削除 |
| `TestAsync` | 現在入力中の PAT で `ListFilesWithPatAsync()` を実行し、接続結果を表示 |

---

## 6. Cloudflare Worker 設計

### 6-1. 概要

`worker/index.js` に実装される Cloudflare Worker。  
Worker 名: `kabumemo-proxy`  
公開 URL: `https://kabumemo-proxy.kabumemo.workers.dev`

#### CORS 設定

```javascript
const CORS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'GET, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type',
};
```

OPTIONS リクエストには 204 No Content を返す。

### 6-2. セッション管理

Yahoo Finance は Crumb + Cookie による認証が必要。  
Worker インスタンス内にセッションをキャッシュして再利用する。

```javascript
let cachedSession = null;  // { crumb, cookies }
```

#### refreshSession() フロー

```
1. finance.yahoo.com/ にアクセス → Cookie 取得
2. レスポンスが同意ページ (csrfToken / collectConsent) の場合:
   - consent.yahoo.com/v2/collectConsent へ POST（同意フォーム送信）
   - Cookie を mergeCookies() でマージ
3. query1/query2 で /v1/test/getcrumb を試行 → crumb を取得
4. 取得成功 → cachedSession = { crumb, cookies }
5. 取得失敗 → Error をスロー
```

#### セッションリフレッシュのトリガー

- `cachedSession === null`（初回）
- API レスポンスが HTTP 401
- v7 quote の result が空配列（crumb 失効の可能性）

### 6-3. エンドポイント詳細

#### GET /api/quote/{code}

Yahoo Finance v7 API で銘柄の現在値・各種指標を取得。

```
URL: query1.finance.yahoo.com/v7/finance/quote
  ?symbols={code}.T&crumb={crumb}&lang=ja&region=JP

フォールバック（result が空 / 認証失敗）:
  1. refreshSession() → v7 再試行
  2. v8 chart API (interval=1d, range=10d) で最新終値取得
  3. v10 quoteSummary (modules=price) にフォールバック
```

#### GET /api/chart/{code}

```
URL: query1.finance.yahoo.com/v8/finance/chart/{code}.T
  ?interval=1d&range=1y&crumb={crumb}

レスポンス変換:
  timestamps[] + indicators.quote[0].close[]
  → [{ date: "YYYY-MM-DD", close: 1234.5 }, ...]
```

#### GET /api/dividends/{code}

```
URL: query1.finance.yahoo.com/v8/finance/chart/{code}.T
  ?events=div&range=5y&interval=1mo&crumb={crumb}

レスポンス変換:
  events.dividends（Unix タイムスタンプ → 支払日）
  → 4月始まり年度（月>=4 → 当年, 月<4 → 前年）でグループ集計
  → [{ year, amount, interimAmount, finalAmount }, ...]
     interimAmount: 7月以降払い（中間配当）
     finalAmount:   6月以前払い（期末配当）
```

#### GET /api/summary/{code}

```
URL: query1.finance.yahoo.com/v10/finance/quoteSummary/{code}.T
  ?modules=assetProfile,calendarEvents&crumb={crumb}

翻訳:
  longBusinessSummary（英語）
  → 450文字チャンクに分割
  → MyMemory API (api.mymemory.translated.net) で逐次翻訳
  → 翻訳失敗時は原文を返す

レスポンス:
  { description, employees, city, country, earningsDate }
```

#### GET /api/disclosures/{code}

```
URL: irbank.net/{code}/tdnet/

HTML から正規表現でパース:
  <tbody> → <tr> → <td>[0]日付 [1]時刻 [2]<a>タイトル</a> → URL

レスポンス:
  [{ date, time, title, url }, ...]
```

#### GET /api/name/{query}

```
数値コードの場合:
  → query1.finance.yahoo.com/v1/finance/search?q={code}.T
  → .T で終わるシンボルのみを返す
  → 0件時: { symbol: "{code}.T" } のダミーを返す

日本語テキストの場合:
  1. finance.yahoo.co.jp/search/?query={query}
     HTML から "code","marketName","name" パターンを正規表現で抽出
  2. 0件時フォールバック: Yahoo Finance US search で .T フィルタ
```

### 6-4. エラーハンドリング

```javascript
try {
  return await handle...();
} catch (err) {
  return new Response(JSON.stringify({ error: err.message }), {
    status: 502,
    headers: { ...CORS, 'Content-Type': 'application/json' },
  });
}
```

---

## 7. ルーティング設計

`App.razor` が `Router` コンポーネントでルーティングを管理。  
アセンブリスキャンにより `Pages/` 配下の `@page` ディレクティブを自動認識。

| URL パターン | コンポーネント | 説明 |
|---|---|---|
| `/` | `Pages/Home.razor` | 銘柄一覧画面 |
| `/stock/{Code}` | `Pages/StockDetail.razor` | 個別銘柄詳細 |
| `/disclosures` | `Pages/Disclosures.razor` | IR情報一覧（`?code=` で銘柄フィルタ） |
| `/settings` | `Pages/Settings.razor` | GitHub PAT 設定 |
| その他 | `Pages/NotFound.razor` | 404 ページ |

### SPA ルーティング（GitHub Pages 対応）

GitHub Pages は SPA のディープリンクに対応していないため、以下の対策を実施。

```
1. 404.html に index.html と同一内容を配置
2. 404.html 内のスクリプトが ?p= パラメータでパスを渡す
3. index.html のスクリプトが ?p= を読み取り history.replaceState() で URL を復元
4. Blazor Router が復元後の URL でルーティングを解決
```

---

## 8. DI（依存性注入）設定

`Program.cs`

```csharp
// Blazor 標準 HttpClient
builder.Services.AddScoped(sp =>
    new HttpClient { BaseAddress = new Uri(builder.HostEnvironment.BaseAddress) });

// localStorage 操作
builder.Services.AddScoped<StockService>();

// GitHub API 直接呼び出し（MD共有）
builder.Services.AddScoped<GitHubService>();

// Yahoo Finance / IR 取得（タイムアウト 10 秒）
builder.Services.AddScoped(_ =>
    new StockApiService(new HttpClient { Timeout = TimeSpan.FromSeconds(10) }));

// 適時開示取得・キャッシュ（タイムアウト 15 秒）
builder.Services.AddScoped<DisclosureService>(sp => new DisclosureService(
    new HttpClient { Timeout = TimeSpan.FromSeconds(15) },
    sp.GetRequiredService<IJSRuntime>()
));
```

---

## 9. スタイル設計

### 9-1. グローバルスタイル（app.css）

| クラス / 要素 | 説明 |
|---|---|
| `.app-header` | スティッキーヘッダー（背景 #1a1a2e、高さ 56px） |
| `.app-main` | メインコンテンツ（min-height: calc(100vh - 56px)） |
| `.card` | 白背景、border-radius:10px、box-shadow |
| `.toolbar` | flex レイアウト（更新ボタン・件数表示） |
| `.table-scroll` | overflow-x: auto（横スクロール対応） |
| `tr.highlight-row` | 背景 #fff3cd、左ボーダー黄色（アラーム行） |
| `.stock-code` | font-weight:700、font-size:13px |
| `.price` | font-weight:600、font-size:15px |
| `.change.up` | 色 #e03535（赤、値上がり） |
| `.change.down` | 色 #1565c0（青、値下がり） |
| `.modal-overlay` | 全画面オーバーレイ（rgba(0,0,0,0.45)） |
| `.modal-box` | 幅 90%、最大幅 480px、border-radius:12px |
| `.detail-header` | 詳細画面ヘッダー（背景 #1a1a2e） |
| `.detail-price` | font-size:28px、font-weight:700 |
| `.ext-link-box` | 外部リンク行（flex、padding:12px 14px） |
| `.error-log-panel` | エラーログ（背景 #b32121、文字 #fff） |
| `.disc-item` | IR情報アイテム（カード形式） |
| `.disc-item-today` | 本日の開示（背景色変更） |
| `.disc-item-new` | 未既読の開示（背景色変更） |
| `.disc-new-badge` | NEW バッジ |
| `.disc-cat-*` | カテゴリ別バッジ色 |
| `.disc-progress-bar` | IR取得プログレスバー |
| `.settings-input` | 設定画面の入力フィールド |
| `.settings-label` | 設定画面のラベル |
| `.pat-status.ok` | PAT 保存済み表示（緑） |
| `.pat-status.warn` | PAT 未設定表示（黄） |
| `.md-content` | MD レンダリングコンテナ |

### 9-2. カラーパレット

| 用途 | カラーコード |
|---|---|
| ヘッダー背景 | `#1a1a2e` |
| 主要テキスト | `#333333` |
| 値上がり | `#e03535` |
| 値下がり | `#1565c0` |
| アクセントブルー | `#0066cc` |
| アラーム行背景 | `#fff3cd` |
| カード背景 | `#ffffff` |
| エラーパネル | `#b32121` |
| 中間配当バー | `#64b5f6` |
| 期末配当バー | `#1e88e5` |
| 現時点ラベル | `#e67e22` |

---

## 10. 外部 API 仕様

| API | エンドポイント | 認証方式 | 用途 |
|---|---|---|---|
| Yahoo Finance v7 | `query1/query2.finance.yahoo.com/v7/finance/quote` | Crumb + Cookie | 現在値・各種指標 |
| Yahoo Finance v8 | `query1/query2.finance.yahoo.com/v8/finance/chart` | Crumb + Cookie | チャート・配当推移 |
| Yahoo Finance v10 | `query1/query2.finance.yahoo.com/v10/finance/quoteSummary` | Crumb + Cookie | 企業概要・決算日 |
| Yahoo Finance Search | `query1.finance.yahoo.com/v1/finance/search` | なし | 銘柄コード検索 |
| Yahoo Finance Japan | `finance.yahoo.co.jp/search/` | なし | 銘柄名検索（日本語） |
| irbank.net | `irbank.net/{code}/tdnet/` | なし | 適時開示スクレイピング |
| MyMemory | `api.mymemory.translated.net/get` | なし | 英日翻訳 |
| GitHub Contents API | `api.github.com/repos/{owner}/{repo}/contents/{path}` | PAT (Bearer) | MD共有ファイル操作 |

### Cloudflare Worker 無料枠

| 制限 | 上限 |
|---|---|
| リクエスト数 | 10万回 / 日 |
| CPU 時間 | 10ms / リクエスト |
| スクリプトサイズ | 1MB |

---

## 11. ビルド・デプロイ設計

### 11-1. ローカル開発

```powershell
cd C:\Users\takes\source\repos\KabuMemo
dotnet run        # http://localhost:5124
dotnet watch      # ホットリロード
```

Worker のローカルテスト：

```powershell
cd worker
npx wrangler dev  # http://localhost:8787
```

ローカルテスト中は `StockApiService.cs` および `DisclosureService.cs` の `CfWorkerBase` を変更：

```csharp
// private const string CfWorkerBase = "https://kabumemo-proxy.kabumemo.workers.dev";
private const string CfWorkerBase = "http://localhost:8787";
```

### 11-2. GitHub Pages 自動デプロイ（GitHub Actions）

`.github/workflows/deploy.yml` によって `master` ブランチへの push を契機に自動実行。

```
1. Checkout
2. Setup .NET 10
3. dotnet publish -c Release
   → bin/Release/net10.0/publish/wwwroot/
4. sed でビルド済み index.html の base href を書き換え
   / → /KabuMemo/
5. actions/upload-pages-artifact でアーティファクトをアップロード
6. actions/deploy-pages で GitHub Pages へ配置
```

GitHub Pages の Source 設定: **GitHub Actions**

### 11-3. Cloudflare Worker デプロイ

```powershell
cd worker
npm install
npx wrangler login   # 初回のみ
npx wrangler deploy
```

---

## 12. 制約・非機能要件

| 項目 | 内容 |
|---|---|
| サーバーサイド処理 | なし（完全クライアントサイド） |
| データ永続化 | ブラウザ localStorage のみ（端末・ブラウザをまたぐ同期は未対応）※MD共有のみ GitHub を使用 |
| 認証・認可 | なし（個人利用前提）。MD共有は GitHub PAT で認証 |
| API タイムアウト | 株価API: 10秒 / IR情報API: 15秒 |
| 株価更新間隔 | 逐次更新・銘柄間 200ms 遅延。最終更新から30分経過で画面遷移時に自動更新 |
| IR情報更新間隔 | 画面遷移時に5分以上経過で自動更新。IR情報一覧画面では5分刻みで自動更新 |
| IR情報キャッシュ | localStorage に保存、TTL 1時間 |
| CORS | Cloudflare Worker がすべてのオリジンを許可 (`*`) |
| MD共有ファイルサイズ | アップロード上限 100KB |
| オフライン | localStorage のデータは参照可能、API 取得は不可 |
| ブラウザ対応 | モダンブラウザ（Chrome / Edge / Safari / Firefox 最新版） |
